{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Anomali IOC Index",
  "description": "Smart deduplication index tracking known IOCs by type for performance optimization",
  "properties": {
    "iocs": {
      "type": "array",
      "description": "Array of known IOC values for this type",
      "items": {
        "type": "string",
        "description": "IOC value (IP, domain, hash, etc.)"
      },
      "maxItems": 500000
    },
    "last_updated": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when this index was last updated"
    },
    "ioc_type": {
      "type": "string",
      "description": "Type of IOCs stored (ip, domain, hash_md5, etc.)",
      "enum": ["ip", "domain", "url", "email", "hash_md5", "hash_sha1", "hash_sha256"]
    },
    "count": {
      "type": "integer",
      "description": "Number of IOCs in this index",
      "minimum": 0,
      "maximum": 500000
    }
  },
  "required": ["iocs"],
  "additionalProperties": false
}